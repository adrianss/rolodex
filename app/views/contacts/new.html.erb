<h1>New contact</h1>

<% form_for(@contact) do |f| %>
<%= f.error_messages %>
<%= render :partial => "form", :locals => { :f => f } %>
<div class="actions">
  <%= f.submit 'Create' %>
</div>
<% end %>

<%= link_to 'Back', contacts_path %>
